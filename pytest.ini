[pytest]
pythonpath =
    .
    src
    scripts
testpaths = tests
addopts = -n auto
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated
markers =
    slow: tests that insert large volumes of data or run full pipeline builds
